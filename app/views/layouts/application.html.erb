<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= full_title(yield(:title)) %></title>

    <%= stylesheet_link_tag    "application", 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/header' %>

    <div class="row">
      <div id="content" class="small-12 columns">
      <% if !flash.empty? %>
        <div id="flash_content" class="row">
        <% flash.each do |message_type, message| %>
          <%= content_tag(:div, message, class: "small-7 small-centered text-center columns alert-box #{message_type}", html: { 'data-alert' => true }) %>
        <% end %>
        </div>
      <% end %>

        <%= yield %>
      </div>
    </div>

    <%= render 'layouts/footer' %>

    <% if Rails.env.development? %>
    <div id="debug_dump" class="row">
      <div class="small-12 columns panel radius">
        <%= debug(params) %>
      </div>
    </div>
    <% end %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  </body>
</html>
